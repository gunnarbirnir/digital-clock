{
  "name": "digital-clock-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "webpack serve",
    "build:app": "webpack --mode production",
    "build:types": "tsc",
    "build": "yarn clean && yarn build:app && yarn build:types",
    "clean": "rm -rf build"
  },
  "dependencies": {
    "@gunnarbirnir/digital-clock": "1.0.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
