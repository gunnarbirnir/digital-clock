{
  "name": "digital-clock-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:app": "echo 'Building app'",
    "build:types": "tsc",
    "clean": "rm -rf build",
    "build": "yarn clean && yarn build:app && yarn build:types"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
