{
  "name": "@gunnarbirnir/digital-clock",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "module": "build/index.esm.js",
  "scripts": {
    "build:package": "rollup -c",
    "build:types": "tsc",
    "build": "yarn clean && yarn build:package && yarn build:types",
    "clean": "rm -rf build"
  },
  "peerDependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
